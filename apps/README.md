# Application Boundary

Runnable applications are currently registered under `artifacts/`. This directory is reserved for future application source when an approved architecture requires a separate application boundary.