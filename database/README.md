# Database Boundary

The shared database package lives in `lib/db`. This directory reserves a future home for migration documentation and database operations. Phase 0 intentionally adds no KFIN business tables or real data.