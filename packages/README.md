# Package Boundary

Reusable libraries belong under `lib/` in the current workspace template. This directory documents the conceptual package boundary for future architecture work; do not create shared packages without a real reuse and ownership need.